{
  "schema_version": 1,
  "name": "novaku-ui-system",
  "purpose": "Agent-operable UI design system entrypoint for the Novaku/Postervia Expo app.",
  "principle": "Correct UI should be the easiest path: use blessed interfaces, recipes, dev gallery states, and verification commands before writing custom layout.",
  "paths_relative_to_repo_root": true,
  "required_before_any_ui_change": [
    "Read novaku-app/ui-system/README.md",
    "Read this manifest",
    "Use components/Screen.tsx for screen composition unless the file is a tiny framed sub-surface inside another Screen",
    "Check docs/MOBILE_PLATFORM_GOTCHAS.md section 9",
    "Add or update a dev gallery state when the UI introduces a new reusable primitive, recipe, or risky state"
  ],
  "foundations": {
    "screen": {
      "path": "components/Screen.tsx",
      "owns": ["ambient background", "safe-area top inset", "floating tab-bar clearance", "keyboard avoidance"],
      "rule": "Route screens compose with Screen instead of re-deriving SafeArea, AppBackground, keyboard, or tab bar padding."
    },
    "layout": {
      "path": "theme/layout.ts",
      "owns": ["floating tab bar height", "bottom inset floor"],
      "rule": "Never copy tab bar geometry into route files. Use getTabBarHeight(bottomInset)."
    },
    "tokens": {
      "path": "theme/tokens.ts",
      "owns": ["colors", "gradients", "radius", "spacing", "shadows", "typography"],
      "rule": "Prefer semantic tokens and blessed primitives over local hex/radius/shadow definitions."
    },
    "i18n": {
      "paths": ["lib/i18n.ts", "locales/en/common.json", "locales/zh/common.json", "locales/de/common.json", "locales/all_translations.ts"],
      "rule": "Any user-visible copy must enter the language system. Dev gallery copy is the exception and stays English-only."
    }
  },
  "blessed_primitives": [
    {
      "name": "Screen",
      "path": "components/Screen.tsx",
      "use_when": "Any route-level screen, full-screen modal body, or tab screen."
    },
    {
      "name": "PageHeader",
      "path": "components/PageHeader.tsx",
      "use_when": "Standard top page title, subtitle, and language/header actions."
    },
    {
      "name": "GlassCard",
      "path": "components/GlassCard.tsx",
      "use_when": "Warm rounded card surface that should match the app visual language."
    },
    {
      "name": "SurfaceCard",
      "path": "components/SurfaceCard.tsx",
      "use_when": "Static card or panel shell where layout must not depend on Pressable callback styles."
    },
    {
      "name": "FeedbackPressable",
      "path": "components/FeedbackPressable.tsx",
      "use_when": "Any pressable needing press feedback: static `style` + `pressedStyle` props. Never use style={({ pressed }) => ...} — nativewind css-interop drops callback-style output on native (GOTCHAS 坑 #7); usePressedFeedback (hooks/usePressedFeedback.ts) is the hook form for custom components."
    },
    {
      "name": "ListRow",
      "path": "components/ListRow.tsx",
      "use_when": "Reusable one-row item with leading icon, copy, optional meta, and optional chevron."
    },
    {
      "name": "StateBlock",
      "path": "components/StateBlock.tsx",
      "use_when": "Empty, loading, error, success, and quiet status states with consistent icon and action layout."
    },
    {
      "name": "GradientButton",
      "path": "components/GradientButton.tsx",
      "use_when": "Primary and secondary command buttons."
    },
    {
      "name": "IconCircleButton",
      "path": "components/IconCircleButton.tsx",
      "use_when": "Compact icon-only actions with consistent hit area and visual style."
    },
    {
      "name": "Pill",
      "path": "components/Pill.tsx",
      "use_when": "Small status/count/category chips."
    },
    {
      "name": "SectionLabel",
      "path": "components/SectionLabel.tsx",
      "use_when": "Section dividers and uppercase section labels."
    },
    {
      "name": "StackedButton",
      "path": "components/StackedButton.tsx",
      "use_when": "Large stacked CTA surfaces in modals and focused flows."
    }
  ],
  "current_recipes": [
    {
      "name": "Chaptered walkthrough",
      "path": "features/guide/chapteredGuide.ts",
      "status": "shared state machine behind the Buddy and Social tours, rendered by /dev/buddy-guide and /dev/social-guide",
      "invariant": "One chapter per surface, armed by the surface that renders it; progress is local-only and each tour keeps its own storage key; the scrim never takes touches and the tour never presses a create/publish button."
    },
    {
      "name": "App update prompts",
      "path": "components/appUpdate/UpdateSheets.tsx",
      "status": "shared recipe, rendered by /dev/app-update",
      "invariant": "The forced-update sheet has no dismiss affordance and swallows Android back; the dismissible ones always offer a later action."
    },
    {
      "name": "OAuth registration confirmation",
      "path": "features/auth/OAuthRegistrationForm.tsx",
      "status": "shared Google/Apple legal-confirmation recipe, rendered by /dev/auth-oauth",
      "invariant": "Provider profile data stays read-only; a shared platform range with lower bound 16+ auto-completes registration after the disclosed provider action, while missing, declined, ambiguous, or unsupported signals fall back to exact birth date and explicit legal consent."
    },
    {
      "name": "Buddy wish creation",
      "path": "components/buddy/BuddyCreateMenu.tsx",
      "status": "expandable entry plus image-first wish recipe, rendered by /dev/buddy-wish",
      "invariant": "The primary plus expands upward into explicit companion and wish actions; wish photos remain optional and user-controlled."
    },
    {
      "name": "Buddy wish photo picker",
      "path": "components/buddy/BuddyPhotoPicker.tsx",
      "status": "shared wish media recipe, rendered by /dev/buddy-wish",
      "invariant": "Up to five images, first image is the cover, and upload failures keep existing form state."
    },
    {
      "name": "OdysseyTaskLineCard",
      "path": "components/recipes/OdysseyTaskLineCard.tsx",
      "status": "extracted recipe, used by app/(tabs)/tasks.tsx",
      "invariant": "Static visual shell must not depend on Pressable pressed-state callback styles."
    },
    {
      "name": "Plaza post card",
      "path": "features/community/CommunityPostCard.tsx",
      "status": "extracted feature recipe, consumed by app/(tabs)/plaza.tsx; rendered at column width by /dev/plaza-card and down one column by /dev/seeded-posts",
      "invariant": "The picture slot's aspect is reserved from the media's stored width/height before layout (lib/cardAspect.ts cardAspectFor, D-078). Never measure the image with onLoad and resize after layout: a card that grows after layout shoves every card below it and the masonry column twitches."
    },
    {
      "name": "Community post image viewer",
      "path": "features/community/CommunityPostImageViewer.tsx",
      "status": "extracted feature recipe, consumed by CommunityPostDetailModal and rendered by /dev/ui-system",
      "invariant": "Multi-image posts expose high-contrast previous/next controls and a counter; single-image posts hide pager controls."
    },
    {
      "name": "Comment row",
      "path": "features/community/CommunityPostComments.tsx",
      "status": "feature recipe"
    },
    {
      "name": "Chat message bubble",
      "path": "app/(tabs)/social.tsx",
      "status": "named export MessageBubble, rendered by /dev/network-resilience; extraction deferred until SOCIAL-UI-002 (codex) lands"
    },
    {
      "name": "Network health banner",
      "path": "components/NetworkHealthBanner.tsx",
      "status": "shared recipe"
    },
    {
      "name": "Walkthrough spotlight",
      "path": "components/guide/SpotlightOverlay.tsx",
      "status": "registry-agnostic container behind all three tours (Plaza via GuideSpotlight, Buddy, Social); rendered by /dev/product-guide, /dev/buddy-guide and /dev/social-guide",
      "invariant": "One Spotlight (components/guide/spotlightParts.tsx) per overlay: the hole is the inner edge of a transparent view's screen-wide border, rounded to the ring's radius, and hole and ring scale on one breathing value (D-120). The scrim never takes touches; the step card never gates its visibility on onLayout; the scrim view keeps overflow hidden so iOS draws the border with CoreAnimation instead of rasterising a screen-sized image."
    },
    {
      "name": "Plaza feed filters",
      "path": "components/community/FeedFilterRow.tsx",
      "status": "shared recipe on the Plaza band (D-086), rendered by /dev/plaza-filters",
      "invariant": "Lives inside the yellow band, not above the list: a filter that scrolls away cannot be undone without scrolling back."
    },
    {
      "name": "Plaza text-post cover",
      "path": "components/community/PostCover.tsx",
      "status": "shared recipe (D-089), rendered by /dev/plaza-cover; consumed by CommunityPostCard for posts without a picture",
      "invariant": "Height is pinned by the card's reserved aspect, so the cover's own layout can never reflow the column."
    },
    {
      "name": "Related posts rail",
      "path": "features/community/RelatedPostsSection.tsx",
      "status": "feature recipe under a post's comments (D-097), rendered by /dev/related-posts",
      "invariant": "Empty, loading and error all render nothing: a section header with no content under it is a promise the data could not keep."
    },
    {
      "name": "Odyssey calendar",
      "path": "components/odyssey/OdysseyCalendarSection.tsx",
      "status": "shared recipe at the top of the Odyssey tab (D-083), rendered by /dev/odyssey-calendar"
    },
    {
      "name": "Plan-when sheet",
      "path": "components/odyssey/PlanWhenSheet.tsx",
      "status": "shared recipe after add-to-odyssey (D-083), rendered by /dev/odyssey-calendar",
      "invariant": "Never blocks the save: both buttons complete the add, they only differ in whether a date rides along."
    },
    {
      "name": "Event date and time field",
      "path": "components/datetime/EventDateTimeField.tsx",
      "status": "composer recipe (D-105); no dev gallery yet, the six-state walk is still open",
      "invariant": "Empty by default, time optional; the three shapes (no date, all-day, timed span) must all stay legal and end must never precede start."
    },
    {
      "name": "Official chip",
      "path": "components/OfficialChip.tsx",
      "status": "byline role on accounts Postervia publishes under (D-065), rendered by /dev/official-chip",
      "invariant": "Reads 'Editor', never 'Official'; every string goes through the real i18n keys so a missing translation shows up in the gallery."
    },
    {
      "name": "Verified badge",
      "path": "components/VerifiedBadge.tsx",
      "status": "account badge drawn from the brand mark (D-063)",
      "invariant": "The star carries no built-in 'verified' meaning, so the accessibility label does the saying."
    },
    {
      "name": "Buddy form field",
      "path": "components/buddy/BuddyFormField.tsx",
      "status": "labelled input with hint, used across the Buddy composer; rendered by /dev/buddy-guide",
      "invariant": "The hint explains what belongs in the box without the tour, so the walkthrough is never the only explanation."
    }
  ],
  "forbidden_patterns": [
    {
      "pattern": "Route screen manually composes AppBackground + SafeArea + ScrollView padding",
      "use_instead": "components/Screen.tsx"
    },
    {
      "pattern": "Hard-coded tab bar bottom padding or copied 64 + inset formulas",
      "use_instead": "theme/layout.ts getTabBarHeight",
      "enforced_by": "npm run verify:ui (literal 3-digit paddingBottom in app/(tabs)/*.tsx fails; social.tsx allowlisted until SOCIAL-UI-002 lands)"
    },
    {
      "pattern": "Essential layout/shell styles placed only in Pressable callback style",
      "use_instead": "FeedbackPressable (or usePressedFeedback) with static style + pressedStyle",
      "enforced_by": "npm run verify:ui (any resting styles inside style={({ pressed }) => ...} fail; app/dev/pressable-probe.tsx allowlisted as the intentional broken specimen)"
    },
    {
      "pattern": "New user-visible copy written directly in a component",
      "use_instead": "lib/i18n.ts and locale files"
    },
    {
      "pattern": "Reusable UI pattern implemented only inside a route after it appears twice",
      "use_instead": "Extract to components/ or features/<area>/ with dev gallery coverage"
    },
    {
      "pattern": "UI declared done without rendering long German, empty, loading, own, and other states",
      "use_instead": "app/dev gallery route plus Expo Web render check"
    },
    {
      "pattern": "Sizing an image frame by measuring the loaded picture and resizing after layout (feed twitch 2026-08-24, detail bounce 2026-09-07)",
      "use_instead": "lib/cardAspect.ts from the media's stored width/height: cardAspectFor for feed cards, detailMediaHeightFor for the detail frame (D-078, D-119)"
    },
    {
      "pattern": "A spotlight scrim cut from rectangles, or a highlight ring animated apart from its hole",
      "use_instead": "components/guide/spotlightParts.tsx Spotlight inside SpotlightOverlay (D-120)"
    }
  ],
  "verification_surfaces": {
    "dev_gallery_hub": "app/dev/index.tsx",
    "routes": [
      {
        "path": "/dev/product-guide",
        "covers": ["GuideSpotlight", "Spotlight: rounded hole + ring on one pulse (D-120)", "step card", "publish confirm", "long German", "empty", "loading", "settings re-entry"]
      },
      {
        "path": "/dev/buddy-guide",
        "covers": ["SpotlightOverlay live with real anchors", "BuddyFormField wish form", "normal", "long German", "empty (no target)", "loading", "self (guided Post asks first)", "other (feed chapter seen)"]
      },
      {
        "path": "/dev/social-guide",
        "covers": ["SpotlightOverlay live with real anchors", "Friends/Buddy step", "long German", "no target (centred card)", "confirm card", "last step"]
      },
      {
        "path": "/dev/app-update",
        "covers": ["UpdateSheets", "what's new", "update available", "update required", "long German", "no highlights"]
      },
      {
        "path": "/dev/odyssey-calendar",
        "covers": ["OdysseyCalendarSection", "PlanWhenSheet", "event + deadline + device entry", "long German titles", "empty month", "loading", "selected day empty with month marked", "plan-when sheet"]
      },
      {
        "path": "/dev/plaza-search",
        "covers": ["initial with recent searches", "Discover board (hidden when empty)", "initial without history", "filters row only after results", "loading", "results with official hit", "zero result with ask CTA", "error"]
      },
      {
        "path": "/dev/plaza-card",
        "covers": ["CommunityPostCard at real column width", "normal", "long German byline", "translated", "read state"]
      },
      {
        "path": "/dev/plaza-cover",
        "covers": ["PostCover for every text cover", "question", "warning", "recommendation", "long German", "Chinese"]
      },
      {
        "path": "/dev/plaza-filters",
        "covers": ["FeedFilterRow on the yellow band", "live tap", "default (nothing chosen)", "a filter deep in the row chosen", "Guide chosen"]
      },
      {
        "path": "/dev/related-posts",
        "covers": ["RelatedPostsSection", "normal", "long German", "empty (renders nothing)", "loading (renders nothing)"]
      },
      {
        "path": "/dev/seeded-posts",
        "covers": ["real D-065 seeded posts through CommunityPostCard", "one full-width column"]
      },
      {
        "path": "/dev/official-chip",
        "covers": ["OfficialChip beside an official author", "human author (chip absent)", "long German name + chip wrapping", "missing account_kind renders as human", "chip beside VerifiedBadge", "real feed card"]
      },
      {
        "path": "/dev/video-post",
        "covers": ["VideoFullscreenModal", "video card badges", "moderation status chips", "long German", "live player + action rail"]
      },
      {
        "path": "/dev/city-location",
        "covers": ["city selection", "location pending", "long German", "empty", "loading", "own", "other"]
      },
      {
        "path": "/dev/auth-oauth",
        "covers": ["OAuthRegistrationForm", "normal", "long German", "empty", "loading", "own", "other", "provider conflict"]
      },
      {
        "path": "/dev/buddy-wish",
        "covers": ["BuddyCreateMenu", "BuddyPhotoPicker", "BuddyPostCard", "normal", "long German", "empty", "loading", "own", "other"]
      },
      {
        "path": "/dev/screen-preview",
        "covers": ["Screen primitive", "normal", "long German", "empty", "loading", "tab bar clearance"]
      },
      {
        "path": "/dev/ui-system",
        "covers": ["SurfaceCard", "ListRow", "StateBlock", "OdysseyTaskLineCard", "CommunityPostImageViewer", "long German stress"]
      },
      {
        "path": "/dev/components",
        "covers": ["GradientButton", "Pill", "GlassCard", "long German stress"]
      },
      {
        "path": "/dev/button-audit",
        "covers": ["button label overflow across locales"]
      },
      {
        "path": "/dev/network-resilience",
        "covers": ["comment pending/failed", "chat pending/failed", "weak network banner"]
      },
      {
        "path": "/dev/pressable-probe",
        "covers": ["GOTCHAS 坑 #7 on-device probe", "callback vs static vs FeedbackPressable"]
      }
    ],
    "commands": [
      "npm run verify:ui",
      "npx tsc --noEmit --pretty false",
      "npm run build:web"
    ],
    "native_follow_up": "Expo Web catches layout drift; native-only surfaces still need real device or Maestro verification."
  },
  "new_ui_workflow": [
    "Identify whether the work is foundation, primitive, recipe, or screen.",
    "Prefer an existing blessed primitive or recipe before writing local layout.",
    "If a local pattern appears twice or owns a risky invariant, extract it into a primitive or recipe.",
    "Add dev gallery states for any new primitive or recipe.",
    "Run the verification commands and report what could not be visually verified."
  ]
}

// The apps use expo-apple-authentication's native button; this one is web only.
export function AppleWebButton(_props: {
  label: string;
  onPress: () => void;
  loading?: boolean;
}) {
  return null;
}
